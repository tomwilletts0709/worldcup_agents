# langchain_agents

This repo is to build a fastapi application with langchain to explore different ways to build AI Agents. 

I will build a standard FastAPI backend but create different way to call agents. I will use a postgres DB across this app. 

I will potentially looks to add in a frontend at some point. 